{
  "name": "abcd",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=production node index.js",
    "dev": "NODE_ENV=development node --watch index.js",
    "test": "NODE_ENV=test node --test",
    "build:ui": "rm -rf dist && cd ../note_frontend && npm run build && cp -r dist ../note_backend",
    "deploy": "fly deploy",
    "deploy:full": "npm run build:ui && npm run deploy",
    "logs:prod": "fly logs",
    "lint": "eslint ."
  },
  "dependencies": {
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "mongoose": "^8.16.4"
  },
  "devDependencies": {
    "@eslint/js": "^9.32.0",
    "@flydotio/dockerfile": "^0.7.10",
    "@stylistic/eslint-plugin-js": "^4.4.1",
    "eslint": "^9.32.0",
    "globals": "^16.3.0",
    "vite": "^7.0.5"
  }
}
